import { DemoWorkflow } from "@/components/demo/demo-workflow";
import "./demo.css";

export const metadata = {
  title: "ORVEX — Live decision",
};

export default function DemoPage() {
  return <DemoWorkflow />;
}
