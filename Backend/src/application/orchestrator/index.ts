/**
 * Orchestrator barrel — Phase 8.
 *
 * The deterministic Security Orchestrator that assembles a decision context from
 * authoritative state + advisory AI intelligence. It produces context, not decisions.
 */
export { SecurityOrchestrator } from "./security-orchestrator.js";
export type {
  OrchestrateInput,
  DecisionContext,
  OrchestratorDeps,
} from "./security-orchestrator.js";
