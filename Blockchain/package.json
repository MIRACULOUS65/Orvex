{
  "name": "@sentinelpay/blockchain",
  "version": "0.1.0",
  "private": true,
  "description": "SentinelPay Blockchain / Execution subsystem — the real ExecutionClient implementation behind the Core execution boundary. Core decides WHETHER an action executes; this package decides HOW it is executed and independently verifies the result.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:anvil": "vitest run test/anvil",
    "test:sepolia": "vitest run test/sepolia"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "viem": "^2.21.0",
    "zod": "^4.6.2"
  }
}
